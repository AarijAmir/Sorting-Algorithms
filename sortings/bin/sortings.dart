void main(List<String> arguments) {
  // List<int> arr = [38, 27, 43, 3, 9, 82, 10];
  // print(arr);
  // HeapSort heapSort = HeapSort(arr);
  // heapSort.sort();
  // heapSort.printArray();

  // print("Original array: $arr");
  // MergeSort mergeSort = MergeSort();

  // mergeSort.mergeSort(arr);

  // print("Sorted array: $arr");
  // List<int> list = [2, 9, 11, 3, 4, 18, 19, 5, 1, 12, 11];

  // ShellSort.shellSort(list);
  // ShellSort.displayArray(arr: list);
  // List<int> list = [9, 7, 5, 4, 2, 1, 3, 6, 8];
  // InsertionSort insertionSort = InsertionSort();
  // insertionSort.insertionSort(list);
  // insertionSort.printArray(list: list);
  // BucketSort bucketSort = BucketSort(list: list);
  // bucketSort.displayArray();
  // bucketSort.bucketSort();
  // bucketSort.displayArray();
  // num i = 23;
  // print(i.runtimeType);
  // i = 34.6;
  // print(i.runtimeType);
  // i = 34;
  // print(i.runtimeType);

  // Object? i = 23;
  // i = 334.4;
  // print(i.runtimeType);

  // Insertion Sort
  // InsertionSort insertionSort = InsertionSort();
  // List<int> list = [10, 3, 2, 5, 8, 4, 3, 1];
  // insertionSort.insertionSort(list: list);
  // insertionSort.printArray(list: list);
  // Bubble Sort
  // BubbleSort bubbleSort = BubbleSort();
  // List<int> arr = [10, 5, 30, 15, 50, 6];
  // bubbleSort.bubbleSort(arr: arr);
  // bubbleSort.displayArray(arr: arr);
  // Section Sort
  // SelectionSort selectionSort = SelectionSort();
  // List<int> arr = [10, 3, 2, 5, 8, 4, 1, 9];
  // selectionSort.selectionSort(arr: arr);
  // selectionSort.displayArray(arr: arr);
}
